// Deprecated: static `models` replaced by dynamic products (DB + app/data/products.json).
// This file remains for compatibility but exports an empty list so the app uses the dynamic source.

export const models = []

export function getModelBySlug() { return null }
